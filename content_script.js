// This script is injected into the page, so it doesn't need to be wrapped.
// It returns the result, which becomes the `result` property in the
// `injectionResults` array in the background script.
document.body.innerText;